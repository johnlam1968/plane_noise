{
  "name": "your-project-name",
  "version": "1.0.0",
  "description": "A Vercel Serverless Function",
  "main": "api/noise_signal.js",
  "scripts": {
    "start": "vercel dev"
  },
  "dependencies": {
    "node-fetch": "2.6.7"  // You can use a specific version like this
    // or simply use the latest version with: "node-fetch": "^3.0.0" 
  },
  "devDependencies": {
    // ... other development-only tools
  }
}
